strict_tdd: true
context: |
  type-key (Monkeyterm) — terminal typing test
  Runtime: Bun v1.3.14
  Language: TypeScript 6.0 (ESM, strict mode)
  UI: OpenTUI (@opentui/core v0.1.103)
  Charting: @crafter/charts v0.2.4
  Architecture: Screaming Architecture — engine/screens/lib/ui/data
  Tests: 115 passing, bun:test
  Strict TDD: enabled (AGENTS.md)
rules:
  proposal:
    require_problem_statement: true
  spec:
    require_acceptance_criteria: true
  design:
    require_tradeoffs: true
  tasks:
    protect_review_workload: true
  apply:
    tdd: true
    test_command: "bun run test"
  verify:
    test_command: "bun run test"
    build_command: "bun run typecheck"
testing:
  detected: "2026-06-03"
  runner:
    command: "bun run test"
    framework: "bun:test"
  layers:
    unit: "bun:test"
    integration: ""
    e2e: ""
  commands:
    unit:
      - scope: "."
        command: "bun run test"
        framework: "bun:test"
    integration: []
    e2e: []
  coverage:
    command: ""
    commands: []
quality:
  lint: ""
  lint_commands: []
  typecheck: "bun run typecheck"
  typecheck_commands:
    - scope: "."
      command: "bun run typecheck"
      framework: "tsc"
  format: ""
  format_commands: []
